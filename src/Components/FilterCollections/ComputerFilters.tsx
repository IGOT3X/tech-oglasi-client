import { useRef } from "react";
import { TFilter } from "../../../types";

const ComputerFilters = ({ setFilter, filter }: { setFilter: Function, filter?: TFilter }) => {
    return(
        <div className="flex gap-[24px]">

        </div>
    )
}

export default ComputerFilters;